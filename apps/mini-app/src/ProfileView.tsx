import { useEffect, useState } from "react";
import { api, jsonBody } from "./api";

type ProfilePayload = {
  photos: Array<{ id: number; takenAt: string }>;
  profile: { name: string; goal: string; level: string; equipment: string; dietPrefs: string; limitations: string; goalWeight: number | null; waterGoalMl: number | null; stepsGoal: number | null; trainingWeekdays: number[]; share: { body: boolean; health: boolean } | null };
  options: Record<string, Array<{ value: string; label: string }>>;
  weekdays: Array<{ value: number; label: string }>;
};

export function ProfileView({ onBack }: { onBack: () => void }) {
  const [data, setData] = useState<ProfilePayload | null>(null);
  const [form, setForm] = useState<ProfilePayload["profile"] | null>(null);
  const [error, setError] = useState<string | null>(null);
  const [saving, setSaving] = useState(false);
  const load = () => { setError(null); api<ProfilePayload>("/api/v2/profile").then((next) => { setData(next); setForm(next.profile); }).catch(() => setError("load")); };
  useEffect(load, []);
  if (error) return <div className="view-stack"><div className="page-title"><h1>Settings</h1></div><div className="card card-muted"><div className="error-state"><strong>Could not load your profile.</strong><button className="button button-ghost" onClick={load}>Retry</button></div></div></div>;
  if (!data || !form) return <div className="view-stack"><div className="skeleton" /><div className="skeleton" /></div>;
  const patch = (value: Partial<ProfilePayload["profile"]>) => setForm((current) => current ? { ...current, ...value } : current);
  const toggleDay = (value: number) => patch({ trainingWeekdays: form.trainingWeekdays.includes(value) ? form.trainingWeekdays.filter((day) => day !== value) : [...form.trainingWeekdays, value].sort((a, b) => a - b) });
  const save = async () => { setSaving(true); setError(null); try { await api("/api/v2/profile", { method: "POST", idempotencyKey: crypto.randomUUID(), body: jsonBody({ ...form, goalWeight: form.goalWeight === null ? null : Number(form.goalWeight), waterGoalMl: form.waterGoalMl === null ? null : Number(form.waterGoalMl), stepsGoal: form.stepsGoal === null ? null : Number(form.stepsGoal) }) }); onBack(); } catch { setError("save"); } finally { setSaving(false); } };
  const photoQuery = () => { const tma = window.Telegram?.WebApp?.initData; if (tma) return `&tma=${encodeURIComponent(tma)}`; return window.location.search.replace(/^\?/, "&"); };
  return <div className="view-stack"><div className="eyebrow">PROFILE · SETTINGS</div><div className="page-title"><h1>Your baseline</h1><button className="text-button" onClick={onBack}>Close</button></div><section className="card"><div className="section-head"><div><span className="eyebrow">IDENTITY</span><h2>Keep it current</h2></div></div><div className="form-grid"><label className="form-field"><span>Name</span><input value={form.name} maxLength={60} onChange={(event) => patch({ name: event.target.value })} /></label><label className="form-field"><span>Goal weight, kg</span><input type="number" min="30" max="300" step="0.1" value={form.goalWeight ?? ""} placeholder="Optional" onChange={(event) => patch({ goalWeight: event.target.value ? Number(event.target.value) : null })} /></label><label className="form-field"><span>Water goal, ml</span><input type="number" min="500" max="8000" step="50" value={form.waterGoalMl ?? ""} placeholder="Auto" onChange={(event) => patch({ waterGoalMl: event.target.value ? Number(event.target.value) : null })} /></label><label className="form-field"><span>Steps goal</span><input type="number" min="1000" max="50000" step="500" value={form.stepsGoal ?? ""} placeholder="8000" onChange={(event) => patch({ stepsGoal: event.target.value ? Number(event.target.value) : null })} /></label></div></section><section className="card"><div className="section-head"><div><span className="eyebrow">TRAINING</span><h2>Weekly rhythm</h2></div></div><div className="form-grid"><label className="form-field"><span>Goal</span><select value={form.goal} onChange={(event) => patch({ goal: event.target.value })}>{(data.options.goal ?? []).map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label><label className="form-field"><span>Experience</span><select value={form.level} onChange={(event) => patch({ level: event.target.value })}>{(data.options.level ?? []).map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label></div><div className="weekday-grid">{data.weekdays.map((day) => <button className={form.trainingWeekdays.includes(day.value) ? "weekday selected" : "weekday"} key={day.value} onClick={() => toggleDay(day.value)}>{day.label.slice(0, 3)}</button>)}</div></section>{data.photos.length > 0 && <section className="card"><div className="section-head"><div><span className="eyebrow">PROGRESS PHOTOS</span><h2>Your timeline</h2></div><span className="tag">{data.photos.length}</span></div><div className="photo-grid">{data.photos.map((photo) => <figure key={photo.id}><img src={`/api/v2/photo?id=${photo.id}${photoQuery()}`} alt={`Progress ${photo.takenAt}`} loading="lazy" /><figcaption>{photo.takenAt}</figcaption></figure>)}</div></section>}{form.share && <section className="card"><div className="section-head"><div><span className="eyebrow">CONSENT</span><h2>Trainer visibility</h2></div></div><label className="check-row"><input type="checkbox" checked={form.share.body} onChange={(event) => patch({ share: { ...form.share!, body: event.target.checked } })} /><span>Share body measurements</span></label><label className="check-row"><input type="checkbox" checked={form.share.health} onChange={(event) => patch({ share: { ...form.share!, health: event.target.checked } })} /><span>Share health and injury context</span></label></section>}{error && <div className="save-note error-note">Could not save changes.</div>}<button className="button button-primary button-wide" onClick={() => void save()} disabled={saving}>{saving ? "Saving…" : "Save profile"}</button></div>;
}
