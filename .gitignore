node_modules/
dist/
*.log
.DS_Store
.wrangler/

# Local secrets — NEVER commit. Only .dev.vars.example is tracked.
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*

# Local Cloudflare config overrides (real account_id / database_id stay out of git).
wrangler.local.toml

# Generated Mini App shell (built from src/webapp by scripts/build-webapp.mjs); public/_headers is committed.
public/app.html

# Local databases / dumps
*.sqlite
*.sqlite3
*.db

# One-off data patches and temp dumps — these touch real user rows, never publish them.
scripts/patch_*.sql
scripts/.*-tmp.sql
scripts/migrations_rebaseline.sql

# Agent tooling and private project instructions
.claude/
CLAUDE.md
skills-lock.json

# Private working material (personal plans, master prompt, generated catalogs)
*.xlsx
promt.txt
products.md
exercises.md
README.uk.md
docs/engagement-lifecycle-plan.md
docs/miniapp-migration.md

# Stray screenshots dropped in the repo root
/*.png

# Local working document, not part of the published project
docs/feature-audit.md
